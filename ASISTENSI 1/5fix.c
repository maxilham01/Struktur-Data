#include <stdio.h>
#include <string.h>

bool pqueue_isEmpty(PriorityQueue *stack) {
    return (stack->_top == NULL);
}

int main (){
    int N;
    int;
    scanf("%d", &N)

    for (long long  i = 0; i < N; i++)
    {
        if (/* condition */)
        {
            /* code */
        }
        
    }
    

}